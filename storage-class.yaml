apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: my-storage  # Provide a meaningful name
provisioner: your-provisioner  # Replace with your storage provisioner, e.g., kubernetes.io/aws-ebs
volumeBindingMode: WaitForFirstConsumer  # Adjust based on your requirements

