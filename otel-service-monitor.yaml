
        apiVersion: monitoring.coreos.com/v1
        kind: ServiceMonitor
        metadata:
          name: opentelemetry
          namespace: prom
        spec:
          endpoints:
          - port: metrics
            interval: 30s
          selector:
            matchLabels:
              app.kubernetes.io/name: opentelemetry-collector
        